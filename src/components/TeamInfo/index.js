import React, {Component} from 'react';

const TeamInfo =  (props) => {
    return (
        <div>
            {
                console.log(props)
            }
        </div>
    )
}

export default TeamInfo;